import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  poweredByHeader: false,
  /* All imagery is local (/public) or CSS — no remote image hosts required. */
};

export default nextConfig;
