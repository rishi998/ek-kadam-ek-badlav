# Optional but recommended on Vercel (Production environment only):
# Your primary public URL so metadata, canonical, and Open Graph use your domain
# instead of the default *.vercel.app hostname.
#
# NEXT_PUBLIC_SITE_URL=https://ekkademekbadlav.org
